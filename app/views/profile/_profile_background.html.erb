  <% if current_user.profile_background.attached? %>
  <%= image_tag(url_for(current_user.profile_background), class: "w-100", style: "max-height: 200px;") %>
  <% else %>
  <%= image_tag("profile_background.png", class: "w-100", style: "max-height: 200px;") %>
  <% end %>